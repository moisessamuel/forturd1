// This script updates the admin password hash in the database
// Run via the /api/admin/init endpoint to apply the new password

console.log("To update the admin password, visit /api/admin/init in your browser or call it via fetch.")
console.log("The new password has been set to: gillette007")
